<div style="width:80%;margin:0 auto;" >
    
    <%= render "form", name: "Editar" %>
    <%
=begin %>%>
    <%= form_for(@article) do |f| %>
        <% @article.errors.full_messages.each do |message| %>
            <div class="be-red white top-space">
                * <%= message %>
            </div>
        <% end %>
        <div class="field">
            <%= f.text_field :title, placeholder: "Titulo", class:"form-control" %>
        </div>
        <div class ="field">
            <%= f.text_area :body, placeholder: "Escribe aqui el articulo", style:"height:250px;", class: "form-control" %>
        </div>
        <div class="field">
            <%= f.submit "Guardar", class:"btn be-red white large-padding" %>
        </div>
    <% end %>

Este es el contenido que se va repetir tanto en el formulario de creacion como el de editar un articulo
<% 
=end %>
</div>


